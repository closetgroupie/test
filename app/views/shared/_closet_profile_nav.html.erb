<ul id="nav">
    <%= filter_link "Closet",
        closet_path(closet),
        closet_path(closet) %>
    <%= filter_link "Profile",
        profile_path(user),
        profile_path(user) %>
    <%= filter_link "Reviews",
        closet_reviews_path(closet),
        closet_reviews_path(closet) %>
</ul>
