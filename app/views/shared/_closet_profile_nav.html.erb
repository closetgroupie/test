<ul id="nav">
    <%= filter_link "Closet",
        closet_path(closet),
        {:controller => :closets, :action => :show} %>
    <%= filter_link "Profile",
        profile_path(user),
        {:controller => :users, :action => :show } %>
    <%= filter_link "Reviews",
        closet_reviews_path(closet),
        closet_reviews_path(closet) %>
</ul>
