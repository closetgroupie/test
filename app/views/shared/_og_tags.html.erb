    <meta property="fb:app_id" content="195906460469934"/>
    <meta property="og:site_name" content="ClosetGroupie" />
    <meta property="og:description" content="Shop in closets you love. Sell from your closet to your friends and fans." />
    <%# TODO: Make a helper method for this.
    <?php if (isset($og_tags) && is_array($og_tags)): foreach ($og_tags as $og_tag): ?>
<meta property="og:<?php echo $og_tag["name"]; ?>" content="<?php echo $og_tag["content"]; ?>" />
    <?php endforeach; endif; ?>
    #%>
